#pragma once

// 클라이언트/서버 공용 설정
#define IP "127.0.0.1"   // 서버 IP (동일 머신이면 loopback)
#define PORT 5001        // pacman_server의 DEFAULT_PORT와 동일
#define HEIGHT 20        // 맵 높이 (pacman_server와 동일)
#define WIDTH 29         // 맵 너비 (pacman_server와 동일)
